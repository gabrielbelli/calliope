[pytest]
# The service directory on sys.path, so `from app import main` works without an
# installed package. This service is not a library and is never pip-installed;
# it is copied into an image and run by uvicorn.
pythonpath = .
testpaths = tests
asyncio_mode = auto
